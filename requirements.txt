# Core inference
vllm==0.2.6
torch>=2.0.0
transformers>=4.34.0
numpy>=1.23.0
tqdm>=4.65.0

# LangGraph pipeline
langgraph>=0.0.40

# Datasets (PopQA long-tail subset)
datasets>=2.14.0

# Optional: ALCE long-form evaluation
# (only needed if running run_compare_eval_longform.py with --task asqa/eli5)
# See https://github.com/princeton-nlp/ALCE for eval.py
rouge_score>=0.1.2
sacrebleu>=2.3.1
nltk>=3.8
